# ── apps/web ─────────────────────────────────────────────────────────────
# Copy to apps/web/.env and fill in. Never commit the real .env.

# Base URL of the Tool Bridge api. Optional (default http://localhost:3000).
VITE_API_URL=http://localhost:3000
